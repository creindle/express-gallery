<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title Express Gallery
    link(rel="stylesheet" type="text/css" href="/styles/styles.css")
  body
    .page-wrap
      hr(class="dash")
      #top-of-gallery
        #header
          li(class="dropdown")
            h1 _ARCHITEKT
            div(class="dropdown-content")
              a(href="/gallery/" + picture.id + "/edit") Edit
              form(id="delete_form" method="POST" action="/gallery/" + picture.id + "?_method=DELETE")
              a(href="javascript:{}" onclick="document.getElementById('delete_form').submit();") Delete
              a(href="/gallery") Back to Gallery
        h2 #{picture.author}

      .pictures
        .picture
          img(src=picture.url width="100%" height="100%")
          p
          span #{picture.description}
          p
          span Copyright #{picture.author}

