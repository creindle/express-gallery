<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title Express Gallery - Login :)
    link(rel="stylesheet" type="text/css" href="/styles/styles.css")
  body
    h1 this is the secret #{user.username}, #{user.password}